{
  "name": "crew-goals",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev --workspace @crew-goals/web\" \"npm run dev --workspace @crew-goals/api\"",
    "dev:web": "npm run dev --workspace @crew-goals/web",
    "dev:api": "npm run dev --workspace @crew-goals/api",
    "build": "npm run build --workspace @crew-goals/web && npm run build --workspace @crew-goals/api",
    "typecheck": "npm run typecheck --workspace @crew-goals/web && npm run typecheck --workspace @crew-goals/api"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "concurrently": "^9.1.2"
  }
}
