# Codex 使用流程复盘

我主要把 Codex 当作一个分阶段的工程协作者来使用，整体流程比较清晰：

## 使用流程
- 先设计并编写了产品文档，并迭代了两版，最终生成`docs/PRD_v2.md`
- 再根据产品文档编写开发计划，并写入 `docs/development-plan/`和`docs/development-plan-v2/`。
- 然后按实施步骤逐步开发
- 每一步完成后，根据 review 报告继续判断问题是否真实存在、是否值得处理，并直接修复。
- 复审通过后再提交代码。
- 最后补充 README，让项目说明和当前实现状态保持一致。

## 用到的 Codex 工具

- 终端命令：用于 `rg` 定位代码和文档、`sed`/`cat` 查看文件、`git status`/`git diff`/`git add`/`git commit` 管理提交，以及 `npm run typecheck`、`npm run build` 做验证。
- `apply_patch`：用于稳定地修改代码和文档，尤其适合小步修复、分段补丁和避免手工编辑带来的上下文偏差。
- `update_plan`：用于维护较大任务的分阶段进度，帮助把“分析、实现、验证、收尾”拆成清晰步骤。
- 并行工具：用于同时读取多个文件、并行跑多个检查或收集上下文，提升排查和验证效率。
- 代码审查式分析：用于判断 review 问题是否真实存在、影响有多大、是否值得处理，以及应该修到什么程度。
- 结构化阅读：通过先看 PRD、开发计划、review guide 和现有实现，再决定改哪些文件，避免只看单点代码就下结论。
- 交付收口：在功能完成后，再回头补 README、开发文档和必要说明，让实现状态和项目说明保持一致。
- 提交管理：在复审通过后统一暂存和提交，把每个阶段的改动收敛成可回顾的 commit。

## 个人结论

- 这套方式最有效的地方，是把 Codex 当成“能读代码、能改代码、能验证代码”的协作对象，而不是只回答问题的助手。
- 对于像 Crew Goals 这种有明确 PRD、实施步骤和 review 流程的项目，这种用法特别顺手。
